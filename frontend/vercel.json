{
  "buildCommand": "cd .. && npm run build:shared && cd frontend && npm run build:vercel",
  "devCommand": "npm run dev",
  "installCommand": "cd .. && npm install && cd frontend && npm install",
  "framework": "vite",
  "outputDirectory": "dist"
}
